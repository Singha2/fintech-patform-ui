# PROJECT TRACKER — pointer

> ⚠️ **This is not the tracker — it is a signpost.** The single cross-repo source of truth for
> "where are we / what's next" across **both** the backend and this UI lives in the backend repo:
>
> ### 👉 `../fintech-platform-backend/docs/PROJECT_TRACKER.md`
>
> Read and update status **there** — one file, both repos. Do **not** track progress in this UI repo
> separately (that is the drift this setup exists to prevent).
>
> UI-side execution detail (the step-by-step wiring plan) stays in [`INTEGRATION_PLAN.md`](INTEGRATION_PLAN.md);
> per-screen endpoint shapes stay in [`API_ALIGNMENT.md`](API_ALIGNMENT.md). Both are *detail* the tracker links to.
